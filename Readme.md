# Joli Biblio

## Install

Install dependency with `cargo` :

```bash
cargo build
```

Run with `cargo` :

```bash
cargo run
```

## Migrations

To run migrations use `sqlx-cli` :

```bash
sqlx migrate run
```
